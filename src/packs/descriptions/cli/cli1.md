## Command Line - 1

We will go over a basic command like task. We will look into creating a directory within our home drive.

* You can use `mkdir` to create a directory that is relative to your home drive. Create a directory within `/root` called `Projects`.


Check your answer using `Check` button.
